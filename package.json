{
  "name": "creimann-eleventy-portfolio",
  "version": "1.0.0",
  "description": "Persönliche Portfolioseite von Christoph B. Reimann, gebaut mit Eleventy (11ty).",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf _site docs",
    "serve": "ELEVENTY_ENV=local npx @11ty/eleventy --serve --port=8080 --watch",
    "build": "ELEVENTY_ENV=production npx @11ty/eleventy",
    "test": "echo \"No tests defined\" && exit 0"
  },
  "keywords": [
    "eleventy",
    "portfolio",
    "static-site",
    "bootstrap5",
    "personal-website",
    "generalist"
  ],
  "author": "Christoph B. Reimann",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
